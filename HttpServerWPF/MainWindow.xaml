<Window x:Class="HttpServerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        prism:ViewModelLocator.AutoWireViewModel="True"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Background="LightYellow"
        Loaded= "Window_Loaded"
        Title="HttpServer" Height="330" Width="800" ResizeMode="NoResize">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Window.InputBindings>
        <MouseBinding MouseAction="LeftClick" Command="{Binding ClearMessageCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <GroupBox Margin="10,5,10,0"
               Header="URI"
               FontSize="20">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Host name:" FontSize="16" Width="80" Margin="0,5,0,0"  />
                        <TextBox Text="{Binding HostName.Value}" FontSize="16" Margin="10,0,0,0" Width="140"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="PortNo:" FontSize="16" Width="80" Margin="0,5,0,0" />
                        <TextBox Text="{Binding PortNo.Value}" FontSize="16" Margin="10,5,0,0" Width="45"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Path:" FontSize="16" Width="80" Margin="0,5,0,0" />
                        <TextBox Text="{Binding Path.Value}" FontSize="16" Margin="10,5,0,0" Width="140"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
            <GroupBox Margin="10,0,10,0" Header="Login information" FontSize="20">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="UserId:" FontSize="16" Width="80" Margin="0,5,0,0" />
                        <TextBox Text="{Binding UserId.Value}" FontSize="16" Margin="10,5,0,0" Width="140"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Password:" FontSize="16" Width="80" Margin="0,5,0,0" />
                        <TextBox Text="{Binding Password.Value}" FontSize="16" Margin="10,5,0,0" Width="140"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </StackPanel>
        <DockPanel Grid.Row="1" LastChildFill="False">
            <Button Content="Save" Command="{Binding SaveCommand}" FontSize="16" Margin="5" Height="30" Width="60" DockPanel.Dock="Left"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Start" Command="{Binding StartCommand}" FontSize="16" Margin="5" Height="30" Width="60"/>
                <Button Content="Stop" Command="{Binding StopCommand}" FontSize="16" Margin="5" Height="30" Width="60"/>
            </StackPanel>
        </DockPanel>
        <TextBox x:Name="LogTextBox" Grid.Column="1" Grid.RowSpan="2" Text="{Binding LogText.Value}"  Margin="5" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
        <Border Grid.ColumnSpan="2" Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="8">
            <TextBlock Text="{Binding StatusMessage.Value}" FontSize="12" Foreground="Black"/>
        </Border>
    </Grid>
</Window>
